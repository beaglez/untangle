'use strict'

const debug = require('debug')('untangle:lib:untangle');
const INPUT_ERROR_MESSAGE = 'please pass in an array of integers as input';

module.exports = function (input) {
  return [`not what i'm looking for`];
};
